{
  "name": "disease-detector-backend",
  "version": "1.0.0",
  "description": "Minimal backend that keeps the Anthropic API key server-side for the chatbot and disease detector",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "@anthropic-ai/sdk": "^0.32.1"
  }
}